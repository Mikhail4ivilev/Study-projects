**Description**:

Model data folder. There will be `main_data.csv`, `train_data.csv`, `test_data.csv` after model data preparation. Training also will add `embedding.csv` and `n_nearest_neighbors.csv` (`n` = number of recommendations) files.
